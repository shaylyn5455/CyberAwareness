<Window x:Class="CyberAwareness_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aries Security Assistant" Height="600" Width="450" Background="#0A0A0A">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,15">
            <TextBlock Text="ARIES v1.0" FontSize="26" FontWeight="Bold" Foreground="#39FF14"/>
            <TextBlock Text="Cyber Security Assistant Hub // ST10295211" FontSize="11" Foreground="#00FF41" Opacity="0.7"/>
        </StackPanel>

        <ListBox x:Name="ChatHistory" Grid.Row="1" Background="#111111" BorderBrush="#39FF14" BorderThickness="1" Padding="10">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,5,0,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="#1A1A1A" BorderBrush="#39FF14" BorderThickness="0,0,0,1" Padding="12" Margin="0,2">
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="#39FF14"/>
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </ListBox.Resources>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UserInput" Height="45" Background="#111111" Foreground="#39FF14" 
                     CaretBrush="#39FF14" BorderBrush="#39FF14" Padding="15,10" 
                     VerticalContentAlignment="Center" FontSize="14" KeyDown="UserInput_KeyDown"/>

            <Button Grid.Column="1" Content="EXECUTE" Width="90" Margin="10,0,0,0" 
                    Background="#39FF14" Foreground="Black" FontWeight="Bold" Click="SendButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00FF41"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
    